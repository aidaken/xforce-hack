---
description: XForce hackathon project context — challenge, git, shared Supabase
alwaysApply: true
---

# xforce-hack

- TAPIA 2026 challenge **[2]**. Product: **ADDY**. Working MVP in `README.md`.
- GitHub: `aidaken/xforce-hack`. Feature branch: `aidar-kenzhebaev`. Do not checkout `main` to edit. Aidar’s “push to prod” = `git push origin HEAD:main` + `vercel --prod --yes` (never force-push).
- Backend: `server/` ingest + OpenRouter chat. UI stub: `public/app.html` (friends replace this — see `docs/frontend.md`).
- **One** Supabase for every branch: project `xforce-hack`, ref `tfmzjvoqsktlzwnrzdzr`, URL `https://tfmzjvoqsktlzwnrzdzr.supabase.co`, dashboard https://supabase.com/dashboard/project/tfmzjvoqsktlzwnrzdzr
- **One** Vercel for every branch: project `xforce-hack`, id `prj_GXZ75tI8NCSyxSXNFiHanqcIp1Zc`, team `team_XS9LEdNQcwp4cYraijcCL30b`, production https://xforce-hack.vercel.app — see `docs/vercel.md` and `config/vercel.json`. GitHub is already linked. Do not create a second Vercel project.
- After clone: `cp .env.example .env` and `supabase link --project-ref tfmzjvoqsktlzwnrzdzr`. See `docs/supabase.md`.
- Never commit `.env` or `SUPABASE_SERVICE_ROLE_KEY`. Use `lib/supabase.js` (`getSupabase()`) with the anon key in client code. Never put the service-role key on Vercel.
- Keep `CLAUDE.md` and `CURSOR.md` updated when conventions change.
