# Local secrets for ADDY. Copy to secrets.toml and fill the key.
# secrets.toml is gitignored — never commit the real key.

[openrouter]
api_key = ""
model = "deepseek/deepseek-v4.1-flash"
base_url = "https://openrouter.ai/api/v1"
